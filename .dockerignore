# Dependencies (reinstalled inside each stage)
node_modules
**/node_modules

# Build outputs (rebuilt inside each stage)
**/dist
**/.next
**/out
**/*.tsbuildinfo

# Git
.git
**/.git

# Environment (injected via docker-compose or secrets manager)
.env
.env.*
!.env.example

# Mobile app (not containerised)
apps/mobile

# Turbo / editor / OS noise
.turbo
**/.turbo
**/.DS_Store
Thumbs.db
*.log

# Coverage
**/coverage
